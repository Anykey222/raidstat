# RaidStat

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python 3.9+](https://img.shields.io/badge/python-3.9+-blue.svg)](https://www.python.org/downloads/)

Программа-ассистент для лидерского состава ArcheAge. Автоматизирует рутину по ведению учета посещаемости и сбору статистики персонажей (убийства, хонор) с помощью распознавания текста (OCR) на скриншотах.

---

## 📺 Видеоинструкция
[Видеогайд по настройке и использованию](https://www.youtube.com/watch?v=Tz78oDmg_y4)

---

## ⚡ Быстрый старт

1. Запустите приложение (`RaidStat.exe`).
2. Перейдите во вкладку **Настройки** и задайте координаты интерфейса (это нужно сделать один раз).
3. Выберите нужный режим (**Посещаемость** или **Статистика**), укажите папку со скриншотами и нажмите кнопку запуска.

---

## ⚙️ Настройки и Калибровка

Правильная настройка — залог точной работы программы. Уделите этому этапу особое внимание.

### 1. Масштаб интерфейса
В настройках выберите масштаб интерфейса, который вы используете в игре (100%, 110%, 120%, 130%). Лошки с меньшим масштабом, купите уже себе нормальный моник (ну или хотя бы переключайте для скриншотов).
* **Рекомендация**: Чем крупнее масштаб, тем выше точность распознавания текста. Если есть возможность, используйте **120%** или **130%** (в основном программа тестировалась на 120%).

### 2. Задание координат (Калибровка)
Это **критически важный** шаг. Программа должна знать, где именно на экране искать данные.
Нажмите кнопку **"Задать"** напротив соответствующего пункта. Откроется окно со скриншотом.
* **Приближение**: Используйте `Ctrl + Колесико мыши` для зума.
* **Выбор точки**: Кликните левой кнопкой мыши.

#### 🎯 Для режима "Посещаемость" (Рейдфрейм)
Вам нужно выбрать **левый верхний угол самой первой ячейки** рейдфрейма (1-1).
* Представьте прицел-перекрестие вашего курсора.
* Горизонтальная и вертикальная линии перекрестия должны **идеально ложиться ("облизывать")** границы прямоугольника первой ячейки.
* Точность должна быть пиксельная. Используйте зум!

#### 🎯 Для режима "Статистика" (Характеристики)
Нужно выбрать **левый верхний угол окна статистики** персонажа.
* Ориентируйтесь на начало "самого черного" фонового прямоугольника окна со статами.
* Аналогично: линии прицела должны точно ложиться на верхний-левый край этого прямоугольника.

> 💡 **Совет**: Включите опцию **"Сохранять отладочные скриншоты"** в настройках. После пробного запуска проверьте папку `debug_screens`. Если на картинках текст обрезан или съехал — скорректируйте координаты.

### 3. Режимы OCR (Распознавание)
Вы можете выбрать один из двух режимов работы движка распознавания:

* **Оффлайн (Offline)**: Использует встроенный Tesseract. Быстро, не требует интернета, но может ошибаться на сложных фонах.
* **Смешанный (Mixed)**: Комбинирует локальный движок и облачное распознавание.
    *   **Преимущества**: Обеспечивает наиболее точное распознавание сложных имен.
    *   **Ограничения**:
        *   Требуется подключение к интернету.
        *   Лимит бесплатного API: **180 запросов в 10 минут**. (Этого обычно хватает на 3-4 рейда).
        *   Иногда сервер может быть недоступен (тогда программа автоматически использует оффлайн метод).
    *   **Ключ API**:
        1. Получите бесплатный ключ здесь: [https://ocr.space/ocrapi/freekey](https://ocr.space/ocrapi/freekey).
        2. В программе выберите режим "Смешанный" и вставьте полученный ключ в появившееся поле.

### 4. Группировка статистики (Таймаут)
Этот параметр определяет временное окно (в минутах), в рамках которого скриншоты считаются одной группой (одним событием/рейдом).
*   **По умолчанию**: 15 минут.
*   **Как использовать**:
    *   Если вы "докликиваете" опоздавших спустя полчаса после основной массы, **увеличьте** это значение (например, до 40-60 минут), чтобы они попали в ту же группу.
    *   Если у вас все пвп заняло 10 минут, **уменьшите** значение (например, до 5 минут), чтобы все скриншоты не были считаны как "начало".

---

## 🛠 Режимы работы

### 🛡 Вкладка "Посещаемость"
Предназначена для создания таблиц посещения рейдов.

**Как это работает:**
1. Вы делаете скриншоты на которых виден рейдфрейм.
2. Программа находит список имен, распознает их и заносит единички (1) в Excel таблицу.
3. Скриншоты будут отсортированы по подпапкам с датой создания скриншота. Рядом с оригинальным скриншотом появится скриншот с припиской _res. Если включена галочка "Открывать скриншоты посещаемости", результирующие скриншоты будут открываться автоматически. На них к распознаным ячейкам появятся подписи ников в том виде как их распознала программа:

| Цвет | Значение |
|------|----------|
| ⬜ Белый | Идеальное распознавание (100% совпало со списокм из эксель). |
| 🟨 Жёлтый | Неточное распознавание (в эксель есть похожее имя). |
| 🟦 Синий | Новое имя, которого не было в базе. |
| 🟥 Красный | Имя не удалось прочитать (`???`). |
| ⬛ Чёрный | Сработала автозамена по правилу из `Замены.txt`. |
Ваша задача проверить, правильно ли прошло распознавание и все ли ники распознаны и при необходимости подправить результаты в экселе. Это все же проще чем заносить посещаемость вручную с нуля.

**Группировка по событиям (Подпапки):**
Вы можете **ПРЕДВАРИТЕЛЬНО** сортировать скриншоты по папкам с названиями событий. Например:
```
Скриншоты/
├── Дракон 01-01-2025/
│   ├── screen1.jpg
│   ├── screen2.jpg
│   └── ...
├── Кракен 01-01-2025/
│   ├── screen3.jpg
│   └── ...
└── просто_рейд.jpg
```
Включите галочку **"Обрабатывать подпапки"**. В этом случае программа создаст в Excel отдельные столбцы с заголовками "Дракон 01-01-2025", "Кракен 01-01-2025" и т.д.
Если было 2 рейда, занести оба скрина в одну подпапку - единственный способ для программы понять что это одно событие.

## 💡 Советы для идеального результата
1. **Крайне желательно** иметь точный и актуальный список мемберов гильдии в файле `Raidstat.xlsx` (первая колонка на каждом листе). Программа будет сверяться с этим списком, что значительно повысит точность распознавания и уменьшит количество ошибок.
2. **Фон**: Старайтесь чтобы рейдфрейм был на однородном фоне (например, небо или земля). Пестрый фон мешает распознаванию.
3. **Здоровье**: Желательно, чтобы у всех в рейде было полное здоровье и не было серых иконок (персонаж не рядом или вылетел).
4. **Формат**: JPG предпочтительнее PNG (как ни странно).

### ✍️ Использование файла Замены.txt (Автозамены)

Если вы замечаете, что один и тот же ник постоянно коверкается одинаковым образом, или, например, у РЛ висит корона и метка так, что в интерфейсе остаются лишь пара букв и программно невозможно понять, кто это — используйте файл `Замены.txt`.

**Как это работает:**
1. Откройте файл `Замены.txt` в корневой папке программы (если его нет — создайте).
2. Каждая строка — это одно правило в формате: `РегулярноеВыражение ПравильноеИмя` (разделенные пробелом).
3. Если распознанный текст совпадает с регулярным выражением, программа автоматически заменит его на указанное имя. В интерфейсе такая ячейка будет подсвечена **черным** цветом.

**Пример записи:**
`^Рл.*$ ИмяЛидера`
*(В этом примере: если распознанный текст начинается на "Рл" и дальше идет любой текст — он будет заменен на "ИмяЛидера")*

**Когда это полезно:**
* Когда корона или иконки меток сильно обрезают ник.
* Когда OCR стабильно и одинаково ошибается в конкретном сложном нике.

### 📊 Вкладка "Статистика"
Сбор данных о хоноре и киллах за прайм.

**Как это работает:**
1. Скриншоты делаются с открытым окном характеристик (Необходимо прокликать всех дд до рейда и после). Крайне желательно чтобы **Ник** персонажа был при этом на темном фоне (не табличка со статами, она всегда на темном, а именно ник, который белым шрифтом). Если все вокруг белое или ярко-зеленое, да с неровной текстурой, лайфхак, вызывайте коня и фотографируйте на фоне промежности, там очень темно...
2. Программа считывает: Никнейм, Очки экипировки (ГС), Хонор, счетчик убийств и т.д.
3. Дельта хонора и убийств за рейд сохраняются во вкладку `Statistics`. Сортировка производится по колонке "очки", которая расчитывается по формуле "Хонор + Фраги * 70".

---

## 🔒 Безопасность и Приватность

### 🛡️ Для тех, кто беспокоится о безопасности
Этот проект полностью **Open Source**. Код открыт, и любой желающий может проверить его на отсутствие вирусов, майнеров и стилеров.

1. **Автоматическая сборка**: Исполняемый файл (.exe), который публикуется в релизах, собирается **автоматически** на серверах GitHub (см. GitHub Actions). Это гарантирует, что в сборку попадает ровно то, что написано в коде.
2. **Проверка через ИИ**: Если вы не программист, вы можете скачать исходный код и попросить любой современный ИИ (ChatGPT, Gemini, Claude) проанализировать его с вопросом: *"Проверь этот код на наличие бэкдоров, вирусов или отправки данных на сторонние сервера"*.
3. **Сторонние компоненты (Tesseract)**: Для удобства в программу уже вложена папка с Tesseract OCR (движок распознавания), чтобы все работало "из коробки". 
   * Если вы не доверяете этим файлам, вы можете удалить папку `tesseract` из директории программы (внутри `_internal/bin`).
   * Затем установите официальную версию Tesseract OCR с надежного источника (например, [UB-Mannheim на GitHub](https://github.com/UB-Mannheim/tesseract/wiki)).
   * **Важно**: `rus.traineddata` и `eng.traineddata` прийдется скачать отдельно (https://github.com/tesseract-ocr/tessdata_best) и положить их в папку `tessdata` внутри директории установки Tesseract.
   * Программа автоматически найдет установленный в системе Tesseract и будет использовать его.

### ⚙️ Принципы работы
*   **Без инъекций**: Программа **не модифицирует** клиент игры, не читает память процесса ArcheAge и не нарушает целостность игровых файлов. Вся работа ведется исключительно с файлами скриншотов на вашем диске.
*   **Конфиденциальность**: Ваши данные никуда не отправляются. Программа работает локально. Исключение — режим "Смешанный OCR" (Mixed), который вы включаете **вручную**. Только в этом режиме маленькие фрагменты скриншотов с именами могут отправляться на публичный API `ocr.space` для улучшения качества распознавания.

---

## 🔄 Перенос данных в Google Таблицы

Многие гильдии ведут "чистовик" в Google Таблицах. Просто копировать (Ctrl+C - Ctrl+V) новые колонки можно только если список людей **абсолютно идентичен** (а по хорощему он должен быть идеинтичен, тогда достаточно будет отсортировать оба списка по имени). Если порядок ников отличается, данные съедут.

### ✅ Массовый перенос (ВПР / VLOOKUP)
Этот способ позволяет перенести **сразу несколько колонок** и автоматически сопоставить данные по никам.

1. **Подготовка**:
   * Откройте `Raidstat.xlsx`, скопируйте всё (Ctrl+A -> Ctrl+C).
   * В вашей Google Таблице создайте временный лист **temp** и вставьте данные туда (Ctrl+V).
   * Допустим, на листе **temp** нужные вам данные начинаются со 2-й колонки (B), а ники в **A**.

2. **Формула**:
   * Перейдите на основной лист. Выберите ячейку в первой колонке, куда нужно вставить данные (например, **F2**, напротив первого ника).
   * Вставьте универсальную формулу:
     `=IFERROR(VLOOKUP($A2; temp!$A:$Z; COLUMN(B1); 0); "")`
   * *Для русской версии таблиц:*
     `=ЕСЛИОШИБКА(ВПР($A2; temp!$A:$Z; СТОЛБЕЦ(B1); 0); "")`

   **Разбор магии**:
   * `СТОЛБЕЦ(B1)` — возвращает "2". Главный трюк в том, что когда вы потянете формулу **вправо**, `B1` превратится в `C1` (вернет "3"), затем в `D1` ("4") и так далее. Вам не нужно менять цифры вручную!
   * `ЕСЛИОШИБКА` — оставит ячейку пустой, если человека нет в списке (вместо ошибки `#Н/Д`).

3. **Заполнение**:
   * **Потяните за уголок ячейки ВПРАВО** на нужное количество колонок (сколько вы хотите перенести).
   * Затем, не снимая выделения, **потяните ВНИЗ** до конца списка людей (или дважды кликните по уголку).

4. **Фиксация (Превращение формул в текст)**:
   * Сейчас в ячейках находятся *формулы*, которые ссылаются на лист **temp**. Если вы удалите лист **temp**, все данные превратятся в ошибки `#ССЫЛКА!` (`#REF!`).
   * Чтобы этого не произошло:
     1. Выделите полученные данные.
     2. Нажмите `Ctrl + C` (Копировать).
     3. Нажмите `Ctrl + Shift + V` (Специальная вставка -> Только значения).
   * **Результат**: Данные отвязались от формул и стали обычным текстом. Теперь лист **temp** можно смело удалять.

---

## 📜 Лицензия
MIT License. Вы можете свободно использовать, копировать и изменять этот код.
